import { SearchElementPipe } from './search-element-pipe';

describe('SearchElementPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchElementPipe();
    expect(pipe).toBeTruthy();
  });
});
